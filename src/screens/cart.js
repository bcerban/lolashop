import React, {Component} from 'react';
import {Text} from 'react-native';
import Layout from '../components/layout';

export default class Cart extends Component {
    render() {
        return <Layout><Text>Hi! This is the cart view.</Text></Layout>
    }
}